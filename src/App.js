import React from "react"
import "./styles/App.css"

// Probando...
const App = () => {

    const registerMode = false

    return (
        (registerMode) ? (
            <div className="page">
                
            </div>
        ) : (
            <div className="page">

            </div>
        )
    )
}

export default App